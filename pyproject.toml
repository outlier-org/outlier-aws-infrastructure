# ~~ Generated by projen. To modify, edit .projenrc.py and run "npx projen".

[tool.poetry]
name = "outlier-aws-cdk-infrastructure"
version = "0.1.0"
description = "Create and deploy an AWS CDK app that will manage the infra for 1 or more AWS account(s) using GitHub actions."
license = "Apache-2.0"
authors = [ "Dobson Dunavant <dobson.dunavant@savvas.com>" ]
readme = "README.md"

  [tool.poetry.dependencies]
  aws-cdk-github-oidc = "*"
  aws-cdk-lib = "2.153.0"
  constructs = "^10.0.5"
  python = "^3.8"

[tool.poetry.group.dev.dependencies]
projen = "0.85.2"
ruff = "*"
pytest = "7.4.3"

[build-system]
requires = [ "poetry-core" ]
build-backend = "poetry.core.masonry.api"
